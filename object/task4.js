// Count the number of properties.

let student = {
  name: "Ariana Grande",
  age: 21,
  city: "Gaibandha",
  isStudent: true,
};

let properties = Object.keys(student);

console.log(properties.length);
